export const sampleNotes = `Binary search trees store values in a sorted structure. 
For each node, values smaller than the node go to the left subtree, and values larger than the node go to the right subtree. 
Searching can be efficient when the tree is balanced, but performance gets worse when the tree becomes too tall. 
AVL trees improve this by rotating nodes when the tree becomes unbalanced. 
A balance factor is used to check whether a node is left-heavy or right-heavy. 
Rotations help keep the tree height close to logarithmic, which improves search, insert, and delete operations.`
